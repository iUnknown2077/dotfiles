# Aliases

# Clear
alias c='clear'

# Fastfetch
alias nf='fastfetch'
alias ff='fastfetch'
alias pf='fastfetch'

# Core utilities
alias ls='eza -a --tree --level=1 --color=always --icons=always --group-directories-first'
alias cp='cp -i'
alias mv='mv -i'
alias mkdir='mkdir -p'

# Power managment
alias shutdown='systemctl poweroff'
alias reboot='systemctl reboot'

# Neovim
alias v='$EDITOR'
alias vim='$EDITOR'
